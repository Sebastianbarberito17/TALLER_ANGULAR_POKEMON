import { ResaltarPokemon2 } from './resaltar-pokemon2';

describe('ResaltarPokemon2', () => {
  it('should create an instance', () => {
    const directive = new ResaltarPokemon2();
    expect(directive).toBeTruthy();
  });
});
